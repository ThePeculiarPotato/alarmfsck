- decompress files and put them back
- encrypt them files
- allow for a 'debug' switch that doesn't delete the original files - libreadline?
- save archives in user's home directory, scan the usual places for it
- allow control over shredding procedure
- port it to autoconf
